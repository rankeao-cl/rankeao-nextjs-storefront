"use client";

// Navbar is now integrated into Header.tsx (bluecard.cl style - single header with nav)
// This component is kept as a no-op to avoid breaking the layout import
export default function Navbar() {
  return null;
}
